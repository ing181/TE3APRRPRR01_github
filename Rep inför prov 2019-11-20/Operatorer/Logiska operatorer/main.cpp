// Logiska operatorer
#include <iostream>
using namespace std;
int main() {
	
	// Logiska operatorer
	// && OCH
	// || ELLER
	// ! ICKE
	
    cout << ( (5!=3) && (4==3) ) << endl; // Är hela uttrycket sant? (Returnerar 1)
    // Utför uttrycken innom paranteserna, slå ihop paranteserna och se vad som händer
	cout << (false) << endl; // Är hela uttrycket sant? (Returnerar 1)
	return 0;
}
